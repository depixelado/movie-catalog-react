import { combineReducers } from 'redux';

// import {
//   UPDATE_MOVIES,
// } from 'actions/MOVIES';

const initialState = {
  movies: []
}

const movies = (state = initialState.movies, action) => {
  switch (action.type) {
    
    default:
      return state
  }
}

export default movies;